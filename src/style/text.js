
import React from 'react'
import { StyleSheet } from 'react-native'

const text = StyleSheet.create({
    button: {
        color: '#3272fc',
		fontWeight: 'bold'
    },
	botMessage: {
		color: 'black',
		fontSize: 15
	},
	userMessage: {
		color: 'white',
		fontSize: 15
	}
})

export default text
